import { defineSnippet } from "../base";
import ParamsComponent from "./params.vue";

export default defineSnippet({
  title: "Convert to GIF",
  keywords: ["gif", "animation"],
  ParamsComponent,
});
